/*
 * Copyright (c) 2019 cognitron.in
 * all rights reserved
 */

package in.cognitron.lms;

import org.simplity.fm.gen.Generator;

/**
 *
 */
public class GenClient {
	private static final String SPEC_ROOT = "/Users/cognitrontechnologies/workspace/ws/banyantree-server/assets/spec/";
	private static final String TS_ROOT = "/Users/cognitrontechnologies/workspace/new_framework/lms/src/app/gen/";

	/**
	 *
	 * @param args
	 */
	public static void main(final String[] args) {
		final long start = System.currentTimeMillis();
		// if (args.length == 0) {
		// Generator.generateForms(SPEC_ROOT, TS_ROOT);
		// } else if (args.length == 2) {
		// Generator.generateForms(args[0], args[1]);
		// } else {
		// System.err.print("Usage: Java GenClient spec_root ts_root");
		// }
		// System.out.println("generated client-sources in " +
		// (System.currentTimeMillis() - start) + "ms");
//		if (args.length == 0) {
//			Generator.generateClientComponents(SPEC_ROOT, TS_ROOT);
//		} else if (args.length == 2) {
//			Generator.generateClientComponents(args[0], args[1]);
//		} else {
//			System.err.print("Usage: Java GenClient spec_root ts_root");
//		}
//		System.out.println("generated client-sources in " + (System.currentTimeMillis() - start) + "ms");
	}
}
